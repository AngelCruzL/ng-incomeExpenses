import { IncomeExpensePipe } from './income-expense.pipe';

describe('IncomeExpensePipe', () => {
  it('create an instance', () => {
    const pipe = new IncomeExpensePipe();
    expect(pipe).toBeTruthy();
  });
});
