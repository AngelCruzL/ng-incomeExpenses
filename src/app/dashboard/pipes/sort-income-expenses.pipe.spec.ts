import { SortIncomeExpensesPipe } from './sort-income-expenses.pipe';

describe('SortIncomeExpensesPipe', () => {
  it('create an instance', () => {
    const pipe = new SortIncomeExpensesPipe();
    expect(pipe).toBeTruthy();
  });
});
